# Roadmap

Open features and fixes for `payload-better-editor`.

- **i18n** — replace all hardcoded English strings; wire `useTranslation` from `@payloadcms/ui` and accept a `labels` override in `BetterEditorConfig`.
- **Foreign-page handling** — when the iframe navigates to a different URL, dim the sidebar + show a banner with a back-to-preview button.
